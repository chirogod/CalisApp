<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:CalisApp.ViewModels"
             x:DataType="viewmodel:HomeViewModel"
             x:Class="CalisApp.Views.HomeView"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#1a3029">

    <ContentPage.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="BackgroundColor" Value="#2d453d"/>
            <Setter Property="Stroke" Value="#4d6b5e"/>
            <Setter Property="StrokeThickness" Value="1"/>
            <Setter Property="StrokeShape" Value="RoundRectangle 20"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Shadow">
                <Setter.Value>
                    <Shadow Brush="Black" Offset="0,4" Radius="5" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <Border StrokeThickness="0"
                    BackgroundColor="#233831"
                    Padding="25,40,25,40"
                    Margin="0,0,0,20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="0,0,30,30"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#facc43" Offset="0,5" Radius="20" Opacity="0.15"/>
                </Border.Shadow>

                <VerticalStackLayout Spacing="25">

                    <Grid ColumnDefinitions="*, Auto">
                        <VerticalStackLayout VerticalOptions="Center">
                            <Label Text="¡Hola!" 
                                   TextColor="#facc43" 
                                   FontSize="16"
                                   FontAttributes="Bold"/>
                            <Label Text="{Binding Usuario.FullName}" 
                                   TextColor="White" 
                                   FontSize="24" 
                                   FontAttributes="Bold"/>
                        </VerticalStackLayout>

                        <Border Grid.Column="1" 
                                StrokeShape="RoundRectangle 25"
                                HeightRequest="54" 
                                WidthRequest="54"
                                Stroke="#facc43"
                                StrokeThickness="2"
                                BackgroundColor="Transparent">

                            <Image WidthRequest="24" Source="perfil32x32.png"/>
                        </Border>
                    </Grid>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15">

                        <Border BackgroundColor="#2d453d" 
                                Stroke="#facc43"
                                StrokeThickness="1" 
                                Padding="15" 
                                Grid.Column="0"
                                StrokeShape="RoundRectangle 15">
                            <VerticalStackLayout Spacing="5">
                                <Label Text="📈 Nivel" TextColor="#8ca096" FontSize="12"/>
                                <Label Text="Principiante" TextColor="White" FontSize="16" FontAttributes="Bold"/>
                            </VerticalStackLayout>
                        </Border>

                        <Border BackgroundColor="#2d453d" 
                                Stroke="#facc43"
                                StrokeThickness="1" 
                                Padding="15" 
                                Grid.Column="1"
                                StrokeShape="RoundRectangle 15">
                            <VerticalStackLayout Spacing="5">
                                <Label Text="📅 Rutina" TextColor="#8ca096" FontSize="12"/>
                                <Label Text="4 días" TextColor="White" FontSize="16" FontAttributes="Bold"/>
                            </VerticalStackLayout>
                        </Border>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <VerticalStackLayout Padding="25,0,25,25" Spacing="15">

                <Label Text="Acceso Rápido" 
                       TextColor="#facc43" 
                       FontSize="18"
                       FontAttributes="Bold"/>

                <Grid ColumnDefinitions="*,*" 
                      RowDefinitions="140, 140" 
                      ColumnSpacing="15" 
                      RowSpacing="15">

                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CardStyle}">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="📅" FontSize="34" HorizontalOptions="Center"/>
                            <Label Text="Anotarse a&#10;Clases" 
                                   TextColor="White" 
                                   FontSize="14" 
                                   FontAttributes="Bold"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource CardStyle}">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="🏋️" FontSize="34" HorizontalOptions="Center"/>
                            <Label Text="Mi Rutina" 
                                   TextColor="White" 
                                   FontSize="14" 
                                   FontAttributes="Bold"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="▶️" FontSize="34" HorizontalOptions="Center"/>
                            <Label Text="Videos" 
                                   TextColor="White" 
                                   FontSize="14" 
                                   FontAttributes="Bold"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                            <Label Text="👤" FontSize="34" HorizontalOptions="Center"/>
                            <Label Text="Mi Perfil" 
                                   TextColor="White" 
                                   FontSize="14" 
                                   FontAttributes="Bold"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Border>

                </Grid>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>